# Dovecot passwd-file for seed mailboxes (SHA-512 crypt format)
# Format: username:password_hash
# Managed automatically by the mailbox provisioner worker (doveadm pw -s SHA512-CRYPT)
# Add seed mailbox credentials here for local development:
#
# Example entry (password: seedpass123):
# seed1@warming.mailwarm.io:{SHA512-CRYPT}$6$rounds=5000$...
#
# In production, the provisioner service writes credentials via doveadm
